global:
  multiregion:
    # unique id of the region. MUST start at 0 for the computation to work correctly. With 2 regions, you would have region 0 and 1.
    regionId: 1
elasticsearch:
  service:
    loadBalancerIP: <IP>
  extraConfig:
    network.publish_host: <IP>
    node.attr.region: 1